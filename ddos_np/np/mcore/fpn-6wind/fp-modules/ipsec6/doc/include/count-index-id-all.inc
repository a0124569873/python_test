.. Copyright 2013 6WIND S.A.

count
   Count the number of entries per hash key in the fast path's internal tables.
index
   Dump the SA index in the fast path's internal SA table per hash key.
id
   Dump a non unique id per hash key to make it easier to read the SA table.
all
   Dump the same info as *dump-sad*, together with the hash key value associated with each SA.
