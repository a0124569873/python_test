.. Copyright 2013 6WIND S.A.

count
   Count number of entries per hash key in fast path internal tables.
index
   Dump SA index in fast path internal SA table per hash key.
id
   Dump an id per hash key, not unique, to make it easier to read the SA table.
all
   Dump same info as *dump-sad*, additionaly with hash key value associated to
   each SA.
